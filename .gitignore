node_modules/
*.log
.DS_Store

# saida de build dos scripts (.zip prontos pra instalar/publicar) -
# nunca precisa ser versionada, gerada sob demanda
dist-extensions/

# modelos de IA (Smart Cleaner ~200MB, Page Translator ~44MB) - grandes
# demais pro git, baixados/copiados localmente na hora de gerar o
# pacote (ver scripts/build-package.js e
# examples/page-translator/download-model.js)
*.onnx
